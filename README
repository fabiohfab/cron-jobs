# Run Application

## With Docker
    - cp .env.example .env #Change .env as you wish or use the default values
    - cp api/.env.example api/.env
    - cp frontend/.env.example frontend/.env
    - docker-compose up -d
    
## Without Docker
### Start server:
    - cd api
    - yarn install
    - cp .env.example .env #Change .env as you wish
    - yarn start

### Start application:
    - cd frontend
    - yarn install
    - cp .env.example .env #Change .env as you wish
    - yarn start